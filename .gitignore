# dependencies
node_modules/
**/node_modules/

# env / secrets
.env
**/.env
*.local

# runtime secrets — plaintext VPN credentials, never commit
server/chap-secrets
server/peers/
**/peers/

# local SQLite database (legacy; contains credentials). App uses MySQL now.
*.db
*.db-shm
*.db-wal

# build output
dist/
**/dist/
build/

# logs
*.log
npm-debug.log*

# release artifacts
*.zip

# os
.DS_Store
